# internal prototypes / backups / scratch — never publish (D104 fix: git add -A once swept these into the public repo)
index_*_staged.html
*.bak.*
*.bak
backups/
es_bars_*.d99.json
es_wave_*.d99.json
.last_public_md5
.DS_Store
# python bytecode
__pycache__/
*.pyc
# guard runtime incident state — private working file, regenerated each run;
# the dashboard reads the guard verdict from trade_state.json:heartbeat.guard,
# never this file. Not seeded into the repo so production starts clean.
freshness_guard_state.json
